import React from 'react';

const Statistics_card = ({phone}) => {
    const {id,picture,title,category,category_bg,card_bg,text_button_bg,description,price}=phone || {};
 

  
  
  
    return (
      <div>piechart</div>
    );
};

export default Statistics_card;